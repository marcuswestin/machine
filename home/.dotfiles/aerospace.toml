# Managed by chezmoi (symlink ~/.aerospace.toml → this file). Edit in-repo; reload AeroSpace (service mode: r).

# Config version for compatibility and deprecations
# Fallback value (if you omit the key): config-version = 1
config-version = 2

# You can use it to add commands that run after AeroSpace startup.
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# AeroSpace is launched by modules/startup.nix instead of its own login item.
start-at-login = false

# Re-stagger Codex/Cursor windows after AeroSpace starts (e.g. restored windows).
#after-startup-command = [
#  'exec-and-forget ${HOME}/code/machine/scripts/aerospace-stagger-app-window.sh --app-id com.openai.codex --workspaces 1,2,3,4,5,6,7,8,9',
#  'exec-and-forget ${HOME}/code/machine/scripts/aerospace-stagger-app-window.sh --app-id com.todesktop.230313mzl4w4u92 --workspaces A,B,C',
#]

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 30

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Disable macOS "Hide application" (cmd-h).
automatically-unhide-macos-hidden-apps = true

# List of workspaces that should stay alive even when they contain no windows,
# even when they are invisible.
# This config version is only available since 'config-version = 2'
# Fallback value (if you omit the key): persistent-workspaces = []
persistent-workspaces = []

# A callback that runs every time binding mode changes
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# See: https://nikitabobko.github.io/AeroSpace/commands#mode
on-mode-changed = []

# on-window-detected runs when a window is first seen (new window / AeroSpace (re)start), not on reload-config.
# Confirm bundle ids with: aerospace list-apps (also avoid a second config at ~/.config/aerospace/aerospace.toml).
# alt-z -> workspace Q (LG UltraFine): Chrome, iPhone/Android simulators.
[[on-window-detected]]
  if.app-id = 'com.google.Chrome'
  run = 'move-node-to-workspace Q'
[[on-window-detected]]
  if.app-id = 'com.apple.iphonesimulator'
  run = 'move-node-to-workspace Q'
[[on-window-detected]]
  # Android Emulator exposes no bundle id to AeroSpace; app name is the QEMU emulator process.
  if.app-name-regex-substring = '^qemu-system-'
  run = 'move-node-to-workspace Q'

# alt-x -> workspace R (LG UltraFine): Spotify, Messages.
[[on-window-detected]]
  if.app-id = 'com.spotify.client'
  run = 'move-node-to-workspace R'
[[on-window-detected]]
  if.app-id = 'com.apple.MobileSMS'
  run = 'move-node-to-workspace R'

# alt-c -> workspace S (LG UltraFine): Apple TV.
[[on-window-detected]]
  if.app-id = 'com.apple.TV'
  run = 'move-node-to-workspace S'

# alt-v -> workspace T (LG UltraFine): Safari.
[[on-window-detected]]
  if.app-id = 'com.apple.Safari'
  run = 'move-node-to-workspace T'

# FaceTime call windows should float instead of participating in tiled layout.
[[on-window-detected]]
  if.app-id = 'com.apple.FaceTime'
  run = 'layout floating'

# Codex: 1st window → workspace 1, 2nd → 2, … (DELL S3225QC 1–9). Cursor: A,B,C,… (Retina Q/W/E project row).
[[on-window-detected]]
  if.app-id = 'com.openai.codex'
  run = [
    'exec-and-forget ${HOME}/code/machine/scripts/aerospace-stagger-app-window.sh --app-id com.openai.codex --workspaces 1,2,3,4,5,6,7,8,9',
  ]
[[on-window-detected]]
  if.app-id = 'com.todesktop.230313mzl4w4u92'
  run = [
    'exec-and-forget ${HOME}/code/machine/scripts/aerospace-stagger-app-window.sh --app-id com.todesktop.230313mzl4w4u92 --workspaces A,B,C',
  ]

# Workspace layout (monitor force-assignment + alt-key bindings):
#   Project slots (sync alt-1…5 → Dell + Retina qwert only):
#     1/q -> 1/A   2/w -> 2/B   3/e -> 3/C   4/r -> 4/D   5/t -> 5/E
#     alt-1…5 sync order: Retina, then Dell (Dell focused last)
#   DELL S3225QC:      1–9 (alt-1…9); alt-ctrl-1…5 = Dell-only (non-sync)
#   Built-in Retina:   A–E QWERT (alt-q…t); X–Z NM, (alt-n,m,comma) — non-project
#   LG UltraFine:      L–P ASDFG (alt-a…g); Q–U ZXCVB (alt-z,x,c,v,b) — apps on ZXCV
#   App auto-assign:   z->Q Chrome/sim; x->R Spotify/SMS; c->S Apple TV; v->T Safari
[workspace-to-monitor-force-assignment]
  1 = 'DELL S3225QC'
  2 = 'DELL S3225QC'
  3 = 'DELL S3225QC'
  4 = 'DELL S3225QC'
  5 = 'DELL S3225QC'
  6 = 'DELL S3225QC'
  7 = 'DELL S3225QC'
  8 = 'DELL S3225QC'
  9 = 'DELL S3225QC'
  A = 'Built-in Retina Display'
  B = 'Built-in Retina Display'
  C = 'Built-in Retina Display'
  D = 'Built-in Retina Display'
  E = 'Built-in Retina Display'
  X = 'Built-in Retina Display'
  Y = 'Built-in Retina Display'
  Z = 'Built-in Retina Display'
  L = 'LG UltraFine'
  M = 'LG UltraFine'
  N = 'LG UltraFine'
  O = 'LG UltraFine'
  P = 'LG UltraFine'
  Q = 'LG UltraFine'
  R = 'LG UltraFine'
  S = 'LG UltraFine'
  T = 'LG UltraFine'
  U = 'LG UltraFine'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
[gaps]
  inner.horizontal = 0
  inner.vertical = 0
  outer.left = 0
  outer.bottom = 0
  outer.top = 0
  outer.right = 0

# 'main' binding mode declaration
[mode.main.binding]

  # --- General (non-workspace) ---
  # Service mode (alt-;), Open iTerm, Fullscreen, Toggle focused-window floating, Toggle layout, Tab workspace, Toggle monitor move, Reload config
  alt-semicolon = ['mode service']
  alt-enter = ['exec-and-forget open -na iTerm']
  alt-shift-enter = ["fullscreen"]
  alt-backtick = ['layout floating tiling']
  alt-slash = ['layout tiles horizontal vertical']
  alt-period = ['layout accordion horizontal vertical']
  alt-tab = ['workspace-back-and-forth']
  alt-shift-tab = ['move-workspace-to-monitor --wrap-around next']
  alt-shift-backtick = ['reload-config']

  # --- Tiling: focus / move / resize ---
  alt-h = ['focus left']
  alt-j = ['focus down']
  alt-k = ['focus up']
  alt-l = ['focus right']
  alt-shift-h = ['move left']
  alt-shift-j = ['move down']
  alt-shift-k = ['move up']
  alt-shift-l = ['move right']
  alt-minus = ['resize smart -75']
  alt-equal = ['resize smart +75']

  # --- Project sync: alt-1…5 → Retina + Dell (Dell focused last); alt-ctrl-1…5 = Dell-only ---
  alt-1 = ['workspace A', 'workspace 1']
  alt-shift-1 = ['move-node-to-workspace 1', 'workspace A', 'workspace 1']
  alt-shift-ctrl-1 = ['move-node-to-workspace 1']
  alt-ctrl-1 = ['workspace 1']
  alt-2 = ['workspace B', 'workspace 2']
  alt-shift-2 = ['move-node-to-workspace 2', 'workspace B', 'workspace 2']
  alt-shift-ctrl-2 = ['move-node-to-workspace 2']
  alt-ctrl-2 = ['workspace 2']
  alt-3 = ['workspace C', 'workspace 3']
  alt-shift-3 = ['move-node-to-workspace 3', 'workspace C', 'workspace 3']
  alt-shift-ctrl-3 = ['move-node-to-workspace 3']
  alt-ctrl-3 = ['workspace 3']
  alt-4 = ['workspace D', 'workspace 4']
  alt-shift-4 = ['move-node-to-workspace 4', 'workspace D', 'workspace 4']
  alt-shift-ctrl-4 = ['move-node-to-workspace 4']
  alt-ctrl-4 = ['workspace 4']
  alt-5 = ['workspace E', 'workspace 5']
  alt-shift-5 = ['move-node-to-workspace 5', 'workspace E', 'workspace 5']
  alt-shift-ctrl-5 = ['move-node-to-workspace 5']
  alt-ctrl-5 = ['workspace 5']

  # --- Focus window <Number DFS> ---
  alt-ctrl-6 = ['focus --dfs-index 5']
  alt-ctrl-7 = ['focus --dfs-index 6']
  alt-ctrl-8 = ['focus --dfs-index 7']
  alt-ctrl-9 = ['focus --dfs-index 8']
  alt-ctrl-0 = ['focus --dfs-index 9']

  # --- DELL S3225QC: workspaces 6–9 (alt = focus, alt-shift = move window + focus, alt-shift-ctrl = move window only) ---
  alt-6 = ['workspace 6']
  alt-shift-6 = ['move-node-to-workspace 6', 'workspace 6']
  alt-shift-ctrl-6 = ['move-node-to-workspace 6']
  alt-7 = ['workspace 7']
  alt-shift-7 = ['move-node-to-workspace 7', 'workspace 7']
  alt-shift-ctrl-7 = ['move-node-to-workspace 7']
  alt-8 = ['workspace 8']
  alt-shift-8 = ['move-node-to-workspace 8', 'workspace 8']
  alt-shift-ctrl-8 = ['move-node-to-workspace 8']
  alt-9 = ['workspace 9']
  alt-shift-9 = ['move-node-to-workspace 9', 'workspace 9']
  alt-shift-ctrl-9 = ['move-node-to-workspace 9']

  # --- Built-in Retina Display: QWERT row -> project workspaces A–E
  # Y/U/I/O keep monitor and workspace-cycle controls below.
  alt-q = ['workspace A']
  alt-shift-q = ['move-node-to-workspace A', 'workspace A']
  alt-shift-ctrl-q = ['move-node-to-workspace A']
  alt-w = ['workspace B']
  alt-shift-w = ['move-node-to-workspace B', 'workspace B']
  alt-shift-ctrl-w = ['move-node-to-workspace B']
  alt-e = ['workspace C']
  alt-shift-e = ['move-node-to-workspace C', 'workspace C']
  alt-shift-ctrl-e = ['move-node-to-workspace C']
  alt-r = ['workspace D']
  alt-shift-r = ['move-node-to-workspace D', 'workspace D']
  alt-shift-ctrl-r = ['move-node-to-workspace D']
  alt-t = ['workspace E']
  alt-shift-t = ['move-node-to-workspace E', 'workspace E']
  alt-shift-ctrl-t = ['move-node-to-workspace E']

  # --- LG UltraFine: ASDFG row -> project workspaces L–P
  # H/J/K/L keep directional focus and move controls above.
  alt-a = ['workspace L']
  alt-shift-a = ['move-node-to-workspace L', 'workspace L']
  alt-shift-ctrl-a = ['move-node-to-workspace L']
  alt-s = ['workspace M']
  alt-shift-s = ['move-node-to-workspace M', 'workspace M']
  alt-shift-ctrl-s = ['move-node-to-workspace M']
  alt-d = ['workspace N']
  alt-shift-d = ['move-node-to-workspace N', 'workspace N']
  alt-shift-ctrl-d = ['move-node-to-workspace N']
  alt-f = ['workspace O']
  alt-shift-f = ['move-node-to-workspace O', 'workspace O']
  alt-shift-ctrl-f = ['move-node-to-workspace O']
  alt-g = ['workspace P']
  alt-shift-g = ['move-node-to-workspace P', 'workspace P']
  alt-shift-ctrl-g = ['move-node-to-workspace P']

  # --- LG UltraFine: ZXCVB row -> app workspaces Q–U
  alt-z = ['workspace Q']
  alt-shift-z = ['move-node-to-workspace Q', 'workspace Q']
  alt-shift-ctrl-z = ['move-node-to-workspace Q']
  alt-x = ['workspace R']
  alt-shift-x = ['move-node-to-workspace R', 'workspace R']
  alt-shift-ctrl-x = ['move-node-to-workspace R']
  alt-c = ['workspace S']
  alt-shift-c = ['move-node-to-workspace S', 'workspace S']
  alt-shift-ctrl-c = ['move-node-to-workspace S']
  alt-v = ['workspace T']
  alt-shift-v = ['move-node-to-workspace T', 'workspace T']
  alt-shift-ctrl-v = ['move-node-to-workspace T']
  alt-b = ['workspace U']
  alt-shift-b = ['move-node-to-workspace U', 'workspace U']
  alt-shift-ctrl-b = ['move-node-to-workspace U']

  # --- Built-in Retina Display: NM, row -> non-project workspaces X–Z
  alt-n = ['workspace X']
  alt-shift-n = ['move-node-to-workspace X', 'workspace X']
  alt-shift-ctrl-n = ['move-node-to-workspace X']
  alt-m = ['workspace Y']
  alt-shift-m = ['move-node-to-workspace Y', 'workspace Y']
  alt-shift-ctrl-m = ['move-node-to-workspace Y']
  alt-comma = ['workspace Z']
  alt-shift-comma = ['move-node-to-workspace Z', 'workspace Z']
  alt-shift-ctrl-comma = ['move-node-to-workspace Z']

  # --- Focused monitor: cycle workspaces (I/U) ---
  alt-i = ['workspace --wrap-around next']
  alt-shift-i = ['move-node-to-workspace --wrap-around next', 'workspace --wrap-around next']
  alt-shift-ctrl-i = ['move-node-to-workspace --wrap-around next']
  alt-u = ['workspace --wrap-around prev']
  alt-shift-u = ['move-node-to-workspace --wrap-around prev', 'workspace --wrap-around prev']
  alt-shift-ctrl-u = ['move-node-to-workspace --wrap-around prev']

  # --- Monitors: focus (O/Y); swap prev/next to match display arrangement ---
  alt-o = ['focus-monitor --wrap-around prev']
  alt-shift-o = ['move-node-to-monitor --focus-follows-window --wrap-around prev']
  alt-y = ['focus-monitor --wrap-around next']
  alt-shift-y = ['move-node-to-monitor --focus-follows-window --wrap-around next']

[mode.service.binding]
  r = ['reload-config', 'mode main']
  esc = ['flatten-workspace-tree', 'mode main']              # reset layout
  f = ['layout floating tiling', 'mode main']                # Toggle between floating and tiling layout
  backspace = ['close-all-windows-but-current', 'mode main']

  alt-shift-h = ['join-with left', 'mode main']
  alt-shift-j = ['join-with down', 'mode main']
  alt-shift-k = ['join-with up', 'mode main']
  alt-shift-l = ['join-with right', 'mode main']

  # Notes and info
  ################

  # [mode.main.binding]
  # All possible keys:
  # - Letters.        a, b, c, ..., z
  # - Numbers.        0, 1, 2, ..., 9
  # - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
  # - F-keys.         f1, f2, ..., f20
  # - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon,
  #                   backtick, leftSquareBracket, rightSquareBracket, space, enter, esc,
  #                   backspace, tab, pageUp, pageDown, home, end, forwardDelete,
  #                   sectionSign (ISO keyboards only, european keyboards only)
  # - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
  #                   keypadMinus, keypadMultiply, keypadPlus
  # - Arrows.         left, down, up, right

  # All possible modifiers: cmd, alt, ctrl, shift

  # All possible commands: https://nikitabobko.github.io/AeroSpace/commands
